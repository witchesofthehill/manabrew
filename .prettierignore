# Auto-generated Tauri artifacts — keep them in the compact form the
# tauri-cli produces; prettier would otherwise expand multi-megabyte
# single-line JSON files into 2000-line diffs every build.
src-tauri/gen/

# The Forge submodule is read-only (parity source of truth, upstream-owned).
# Never reformat it — a stray `prettier --write` here dirties hundreds of its
# docs/json/yml files. Restore with `git -C forge restore .` if it happens.
forge/

# release-please owns this file; reformatting it churns every release PR.
CHANGELOG.md

# gen-protocol-doc-types.mjs owns this file; CI diffs it against the raw
# generator output, so a prettier pass makes the check permanently stale.
website/src/generated/
.agents/skills/
