export const DEV_SECTION = "rounded-xl border border-border/70 bg-muted/15 p-3 shadow-sm" as const;

export const DEV_SECTION_HEADING =
  "text-xs font-semibold uppercase tracking-[0.14em] text-muted-foreground" as const;

export const DEV_CONTROL_BUTTON =
  "rounded-md border px-2.5 py-2 text-xs font-medium transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring" as const;

export const DEV_CONTROL_ACTIVE = "border-primary bg-primary/10 text-primary" as const;

export const DEV_CONTROL_INACTIVE =
  "border-border/70 text-muted-foreground hover:bg-accent/50 hover:text-foreground" as const;
