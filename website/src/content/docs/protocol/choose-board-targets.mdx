---
title: "chooseBoardTargets"
description: Ask the player to choose targets on the board for a spell or ability.
---

import ProtocolType from "../../../components/ProtocolType.astro";

Sent when a spell or ability needs targets chosen from the board - players,
permanents, or spells on the stack. For example, picking the creature or player
Lightning Bolt will deal 3 damage to.

## Arguments

`input.type` is `"chooseBoardTargets"`. The remaining fields:

<ProtocolType slug="choose-board-targets" />

Each `candidates` entry is a `TargetRef`:

| Field  | Type                            | Description                        |
| ------ | ------------------------------- | ---------------------------------- |
| `kind` | `"player" \| "card" \| "spell"` | What kind of object the target is. |
| `id`   | `string`                        | Id of the player/card/spell.       |

## Response

```ts
type ChooseBoardTargetsOutput = { type: "boardTargets"; chosen: TargetRef[] };
```

Return the chosen `TargetRef`s - between `minTargets` and `maxTargets` of them,
drawn from `candidates`.

## Example

```json
{
  "promptId": 5,
  "decidingPlayerId": "player-0",
  "sourceCard": {
    "id": "hand-2",
    "identity": {
      "name": "Lightning Bolt",
      "setCode": "M11",
      "cardNumber": "149",
      "isToken": false
    },
    "color": "R",
    "manaCost": "{R}",
    "cmc": 1,
    "types": ["Instant"],
    "subtypes": [],
    "supertypes": [],
    "power": null,
    "toughness": null,
    "text": "Lightning Bolt deals 3 damage to any target.",
    "controllerId": "player-0",
    "ownerId": "player-0",
    "tapped": false,
    "keywords": [],
    "counters": {},
    "damage": 0,
    "summoningSick": false,
    "isDoubleFaced": false,
    "isTransformed": false,
    "isFaceDown": false,
    "isBestowed": false,
    "phasedOut": false,
    "exerted": false
  },
  "input": {
    "type": "chooseBoardTargets",
    "candidates": [
      { "kind": "player", "id": "player-1" },
      { "kind": "card", "id": "bf-7" }
    ],
    "hostile": true,
    "intent": "damage",
    "minTargets": 1,
    "maxTargets": 1,
    "chosenTargets": 0,
    "label": "Damage"
  }
}
```

```json
{
  "kind": "response",
  "promptId": 5,
  "action": {
    "type": "chooseBoardTargets",
    "output": {
      "type": "boardTargets",
      "chosen": [
        {
          "kind": "card",
          "id": "bf-7"
        }
      ]
    }
  }
}
```
