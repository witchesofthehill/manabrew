---
title: "chooseFromSelection"
description: Ask the deciding player to choose from a list of options.
---

import ProtocolType from "../../../components/ProtocolType.astro";

Sent to choose the modes of a modal spell: a charm, a Command.

Each option has a `weight`. The weights of the chosen options must sum to
between `minTotal` and `maxTotal`. For an ordinary modal spell every weight is
1, so the totals are counts. Pawprint cards ("choose up to five \{P\} worth of
modes") use the weights as pawprint costs. An option with `canRepeat` can be
chosen more than once.

## Arguments

`input.type` is `"chooseFromSelection"`. The remaining fields:

<ProtocolType slug="choose-from-selection" />

## Rendering

The control depends on `maxTotal`.

<figure class="prompt-figure">
  <img src="/protocol/choose-from-selection.webp" alt="A list of single-choice option buttons" />
  <div class="prompt-figure__caption">
    One choice: buttons. Clicking one sends it.
  </div>
</figure>

<figure class="prompt-figure">
  <img
    src="/protocol/choose-from-selection-multi.webp"
    alt="Option rows with checkboxes and a confirm button"
  />
  <div class="prompt-figure__caption">
    Several choices: a checkbox per option and a counter. The player picks between `minTotal`
    and `maxTotal` and confirms.
  </div>
</figure>

<figure class="prompt-figure">
  <img
    src="/protocol/choose-from-selection-cost.webp"
    alt="Mode rows prefixed with pawprint pips and a running total against a budget"
  />
  <div class="prompt-figure__caption">
    Weighted modes with `canRepeat`: pawprint pips and a running total against the budget.
  </div>
</figure>

Clicking a `canRepeat` option adds a copy. Its row shows the count and a
stepper to remove copies. The counter shows the weight total against
`maxTotal`.

Past six options the list becomes a type-to-filter field.

## Response

```ts
type ChooseFromSelectionOutput = { type: "selectionDecision"; chosenIndices: number[] };
```

The indices into `options` of the chosen modes. An index repeats only when
its option has `canRepeat`. The weights sum to between `minTotal` and
`maxTotal`.

## Example

```json
{
  "promptId": 7,
  "decidingPlayerId": "player-0",
  "input": {
    "type": "chooseFromSelection",
    "presentation": {
      "title": "Choose one or both",
      "targets": []
    },
    "options": [
      { "label": "Destroy target artifact.", "weight": 1, "canRepeat": false },
      { "label": "Destroy target enchantment.", "weight": 1, "canRepeat": false }
    ],
    "minTotal": 1,
    "maxTotal": 2
  }
}
```

```json
{
  "kind": "response",
  "promptId": 7,
  "action": {
    "type": "chooseFromSelection",
    "output": {
      "type": "selectionDecision",
      "chosenIndices": [
        0
      ]
    }
  }
}
```
